import styled from "styled-components";

export const StyledComboLayout = styled.div`
    display: flex;
    flex-direction: column;
`;

export const StyledDataTable = styled.div`
    maxWidth: '100%';
`;