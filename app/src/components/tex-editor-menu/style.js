import styled from "styled-components";

export const StyledTexEditorMenu = styled.div`
    display: flex;
    justify-content: space-around;
    margin: 10px;
    .menuItem:hover {
        cursor: pointer;
    }
`;